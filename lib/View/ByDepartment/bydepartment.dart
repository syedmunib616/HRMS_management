import 'package:flutter/material.dart';

class ByDepartment extends StatelessWidget {
  const ByDepartment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}