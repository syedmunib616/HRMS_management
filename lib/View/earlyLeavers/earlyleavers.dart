import 'package:flutter/material.dart';

class EarlyLeaves extends StatelessWidget {
  const EarlyLeaves({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}